# Converts a raw candidate dict + role_model + cosine_sim into a flat dict of 12+ float features (0.0–1.0) used by scorer.py.
# This is the primary driver of ranking quality — all signal engineering lives here.
