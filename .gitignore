# Added by code-review-graph
.code-review-graph/

# Python virtual environment
.venv/

# Candidate data — not committed (large / proprietary)
data/

# Competition bundle — large files, not tracked in git
bundle/

# Large binary artifacts — generated by precompute.py, not tracked in git
artifacts/embeddings.npy
artifacts/candidate_ids.npy
artifacts/sample_embeddings.npy
artifacts/sample_candidate_ids.npy

# Dev-run submission outputs — regenerable, not tracked in git
submission_sample.csv
submission_ownership.csv

CLAUDE.md
# Python cache
__pycache__/
*.pyc
*.pyo

# Scratch and intermediate outputs
submission_full.csv
submission_full_test.csv
submission.csv
FitRank.csv

# Environment / secrets
.env
*.env
.claude/